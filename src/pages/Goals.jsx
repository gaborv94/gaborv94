import { useApp } from '../context/AppContext';
export default function Goals(){const{goals,setGoals,metrics}=useApp();const fields=[['targetWeight','Peso objetivo'],['weeklyCalories','Calorías semanales'],['workoutsPerWeek','Entrenamientos por semana'],['weeklyMinutes','Tiempo semanal']];return <><h1>Metas</h1><div className="card"><div className="card-body row g-3">{fields.map(([k,l])=><div className="col-md-6" key={k}><label className="form-label">{l}</label><input className="form-control" type="number" value={goals[k]} onChange={e=>setGoals({...goals,[k]:+e.target.value})}/></div>)}<div className="col-12"><h4>Cumplimiento</h4><div className="progress"><div className="progress-bar bg-warning" style={{width:`${Math.min(100,metrics.weekCalories/goals.weeklyCalories*100)}%`}}>{Math.round(metrics.weekCalories/goals.weeklyCalories*100)}%</div></div></div></div></div></>}
