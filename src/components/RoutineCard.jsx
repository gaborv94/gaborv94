import { Link } from 'react-router-dom';
export default function RoutineCard({routine}){return <div className="card routine-card h-100"><div className="card-body d-flex flex-column"><span className="badge bg-warning text-dark align-self-start">{routine.tag}</span><h4 className="mt-3">{routine.name}</h4><p className="text-secondary">{routine.description}</p><p><b>Duración:</b> {routine.duration}</p><div className="mb-3">{routine.objective.map(o=><span className="pill" key={o}>{o}</span>)}</div><Link className="btn btn-orange mt-auto" to={`/workout/${routine.id}`}>Entrenar</Link></div></div>}
